<Page
    x:Class="OpendeurdagApp.View.NewsFeed"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpendeurdagApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <PivotItem Header="Newsfeed">
            <ListView Name="ListViewNewsFeed" ItemsSource="{Binding NewsfeedList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Tag}"></TextBlock>
                            <TextBlock Text="{Binding Titel}"></TextBlock>
                            <TextBlock Text="{Binding Inhoud}"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </PivotItem>

        <PivotItem Header="Inschrijven">
            <StackPanel>
                <TextBlock Text="Schrijf je hier in"></TextBlock>
            </StackPanel>
        </PivotItem>
    </Pivot>
</Page>
